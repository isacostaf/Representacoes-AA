# SMTP
SMTP_HOST= host do smtp
SMTP_PORT= porta do smtp

# Credenciais da conta remetente
SMTP_USER = remetente@exemplo.com.br
SMTP_PASSWORD = senha de app remetente
EMAIL_FROM = remetente@exemplo.com.br

# Destinatarios separados por virgula
EMAIL_TO = destinatario@exemplo.com.br
